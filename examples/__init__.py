"""Runnable examples for KEP-7 v2 catalog-native dataset validation.

This package exists so that validator class paths such as
``examples.schemas.CompaniesSchema`` resolve via ``kedro.utils.load_obj``
when the repository root is on ``sys.path``.
"""
