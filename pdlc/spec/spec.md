# Product spec

Requirements live under `themes/` by global topic. See `themes.toml` and
`pdlc spec theme list`. Each requirement is a file `themes/<topic>/RNNNN-slug.md`.

Use `pdlc spec show RNNNN` to read a requirement. Plans propose changes via
`spec_delta.md`; merge with `pdlc plan merge` after review.
